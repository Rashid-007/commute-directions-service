# Commute Service - Commute Direction Service

This service offers a route direction based on google maps data

## Build
`mvn package`

## Local docker image build
 `mvn package docker:build "-Ddocker.image.tag=local"`

## Run
` mvn spring-boot:run`
